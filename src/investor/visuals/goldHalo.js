/**
 * The investor accent colour.
 *
 * The halo alpha and column-height curves that lived here drove a ground-clamped
 * ellipse and a cylinder; markers.js draws the gold sprite and beacon instead.
 */
export const GOLD = Object.freeze({
  r: 0.93,
  g: 0.74,
  b: 0.22,
});
